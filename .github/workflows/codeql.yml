name: "CodeQL Analysis"

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]
  schedule:
    - cron: '0 0 * * 0'  # análisis semanal

jobs:
  analyze:
    name: "CodeQL Scan"
    runs-on: ubuntu-latest

    permissions:
      actions: read
      contents: read
      security-events: write

    strategy:
      fail-fast: false
      matrix:
        language: ["javascript", "python"]  # ajusta según tu stack

    steps:
      - name: "🧾 Checkout repository"
        uses: actions/checkout@v4

      - name: "🧬 Initialize CodeQL"
        uses: github/codeql-action/init@f5622823ace73c9b8ca6b1cf1cf3006cb894565a
        with:
          languages: ${{ matrix.language }}

      - name: "⚙️ Autobuild project"
        uses: github/codeql-action/autobuild@v2

      - name: "🔍 Run CodeQL analysis"
        uses: github/codeql-action/analyze@v2

